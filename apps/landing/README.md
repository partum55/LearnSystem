# LearnSystem Landing Page

This folder is a placeholder for the public landing/marketing site (learnsystem.app).
Currently, the root domain may redirect to `app.learnsystem.app` or point to the same Next.js application in `apps/web`.
