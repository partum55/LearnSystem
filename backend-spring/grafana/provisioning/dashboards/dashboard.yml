apiVersion: 1

providers:
  - name: 'LMS Dashboards'
    orgId: 1
    folder: 'LMS'
    folderUid: 'lms-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

